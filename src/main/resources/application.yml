spring:
  application:
    name: RabbitMqConsumer

server:
  port: 8083

app:
  rabbitmq:
    username: admin
    password: admin
    virtual-host: virtual-host
    queue: test-queue
    exchange: exchange
    rooting-key: test-routing-key
